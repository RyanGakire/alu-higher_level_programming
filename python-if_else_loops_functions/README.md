# 0x01. Python - if/else, loops, functions

## Description

This project covers the fundamentals of control flow in Python, including conditionals (`if/elif/else`), loops (`for`, `while`), and basic function definitions. It is part of the ALX Software Engineering curriculum.

## Learning Objectives

By the end of this project, you should be able to explain:

- Why indentation is so important in Python
- How to use `if`, `if...else`, and `elif` statements
- How to use comments in your code
- How to assign values to variables
- How to use `while` and `for` loops
- How Python's `for` loop differs from C's
- How to use `break` and `continue` statements
- How to use `else` clauses on loops
- What the `pass` statement does and when to use it
- How to use the `range` function
- What a function is and how to use one
- What a function returns if it has no `return` statement
- The scope of variables
- What a traceback is
- What the arithmetic operators are and how to use them

## Requirements

- Allowed editors: `vi`, `vim`, `emacs`
- All files are interpreted/compiled on Ubuntu 20.04 LTS using Python 3 (`python3`)
- All files end with a new line
- The first line of all files is exactly `#!/usr/bin/python3`
- A `README.md` file at the root of the project folder is mandatory
- Code follows **pycodestyle** (version 2.8.*)
- All files must be executable

## Tasks

| File | Description |
|------|-------------|
| `0-positive_or_negative.py` | Prints whether a random number is positive, negative, or zero |
| `1-last_digit.py` | Prints the last digit of a random number with conditions |
| `2-print_alphabet.py` | Prints the lowercase ASCII alphabet without a newline |
| `3-print_alphabt.py` | Prints the lowercase alphabet, excluding `q` and `e` |
| `4-print_hexa.py` | Prints numbers 0–98 in decimal and hexadecimal |
| `5-print_comb2.py` | Prints numbers 00–99 separated by commas |
| `6-print_comb3.py` | Prints all unique two-digit combinations |
| `7-islower.py` | Function that checks if a character is lowercase |
| `8-uppercase.py` | Function that prints a string in uppercase |
| `9-print_last_digit.py` | Function that prints and returns the last digit of a number |
| `11-pow.py` | Function that returns `a` to the power of `b` |
| `12-fizzbuzz.py` | Function that prints FizzBuzz from 1 to 100 |

## Author

**RyanGakire** — Software Engineering Student
